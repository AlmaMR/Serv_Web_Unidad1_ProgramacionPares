// Recorriendo arreglos con forEach
// Dado un arreglo let numeros = [2, 4, 6, 8, 10], usa forEach para
// imprimir cada número en la consola.

// Declaramos un arreglo que contenga los numeros solicitados
let arreglo = [2, 4, 6, 8, 10];

// Creamos un forEach, este recorrera todo el arreglo
arreglo.forEach(numero => {
    console.log(numero);
    // Imprimimos 
});